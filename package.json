{
  "private": true,
  "scripts": {
    "start": "npm --prefix web run start",
    "start:open": "npm --prefix web run start:open",
    "build": "npm --prefix web run build",
    "test": "npm --prefix web run test",
    "deploy:gh": "npm --prefix web run deploy:gh"
  }
}